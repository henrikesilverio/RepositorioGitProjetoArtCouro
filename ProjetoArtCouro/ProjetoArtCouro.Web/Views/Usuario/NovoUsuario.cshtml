@model  ProjetoArtCouro.Model.Models.Usuario.UsuarioModel

@Html.Partial("_UsuarioView", Model)

@section Scripts{
    <script src="~/js/app/Usuario/Usuario.js"></script>
    <script>
        Portal.Mask();
        Portal.NovoUsuario({
            UrlDados: '@Url.Action("NovoUsuario", "Usuario")',
            AlertaMensagensSeletor: "#AlertaMensagens",
            $BotaoSalvar: $("#SalvarUsuario"),
            $Formulario: $("#formularioUsuario")
        });
    </script>
}